package com.techprimers.db.Blockchain;

import com.techprimers.db.model.Voter;

import java.util.Arrays;

public class BlockChainExample {

    Voter voter;
        BlockChainExample(Voter voter)
        {
            this.voter=voter;
        }

        //Transaction transaction1 = new Transaction(voter);

       /* public void executeblock() {
            Block firstBlock = new Block(0, Arrays.asList(transaction1));
            System.out.println(firstBlock.hashCode());
        }*/

}
